apiVersion: 1

providers:
  - name: strata-bridge
    orgId: 1
    folder: Strata Bridge
    folderUid: strata-bridge
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

[package]
name = "strata-bridge-sp1-guest-builder"
version = "0.1.0"
edition = "2021"

[features]
default = []
docker-build = []

[build-dependencies]
sp1-build = "6.2.0"
ssz.workspace = true
strata-bridge-proof = { path = "../../crates/proofs/bridge-proof" }
